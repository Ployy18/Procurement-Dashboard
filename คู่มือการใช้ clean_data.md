# คู่มือการใช้งาน Data Cleaning Service

## ภาพรวม

`DataCleaningService` เป็นบริการสำหรับทำความสะอาดและมาตรฐานข้อมูลการจัดซื้อจัดจ้าง โดยรับข้อมูลดิบจากไฟล์ Excel/CSV และแปลงเป็นข้อมูลที่มีโครงสร้างสม่ำเสมอ

## โครงสร้างข้อมูล

### ข้อมูลนำเข้า (RawDataRow)
```typescript
interface RawDataRow {
  [key: string]: string | number | null | undefined;
}
```

### ข้อมูลส่งออก (CleanedDataRow)
```typescript
interface CleanedDataRow {
  date: string;           // วันที่ (YYYY-MM-DD)
  poNumber: string;       // เลขที่ PO
  supplierName: string;   // ชื่อผู้ขาย
  itemDescription: string; // รายละเอียดสินค้า
  quantity: number;       // จำนวน
  unit: string;           // หน่วย
  projectCode: string;    // รหัสโครงการ
  unitPrice: number;      // ราคาต่อหน่วย
  totalPrice: number;     // ราคารวม
  vatRate: string;        // อัตราภาษี
  engineerName: string;   // ชื่อวิศวกร
  category: string;       // หมวดหมู่
  sourceSheet?: string;   // แหล่งข้อมูล
  _isHead?: boolean;      // 标记是否为Head行
  _isLine?: boolean;      // 标记是否为Line行
  _headPO?: string;       // Head行的PO号
}
```

### ข้อมูลหลายตาราง (MultiTableData)
```typescript
interface MultiTableData {
  procurement_data: CleanedDataRow[];     // ข้อมูลรวม (Line data)
  procurement_head: CleanedDataRow[];     // ข้อมูลส่วนหัว PO
  procurement_line: CleanedDataRow[];     // ข้อมูลรายการ PO
  suppliers_master: { name: string; last_seen: string }[];
  categories_master: { name: string; description: string }[];
  upload_logs: {
    timestamp: string;
    filename: string;
    row_count: number;
    status: string;
    sheets_processed?: number;
    sheet_details?: { sheet: string; rows: number }[];
  }[];
  dataBySheet?: Record<string, CleanedDataRow[]>; // ข้อมูลจัดกลุ่มตามแผ่นงาน
}
```

## ฟังก์ชันหลัก

### 1. `cleanData(rawData: RawDataRow[]): CleanedDataRow[]`
ฟังก์ชันหลักสำหรับทำความสะอาดข้อมูล

**ขั้นตอนการทำงาน:**
1. กรองข้อมูลที่ไม่ถูกต้องออก (`isValidRow`)
2. ประมวลผลแถวที่ถูกต้อง (`processRow`)
3. คืนค่าข้อมูลที่ทำความสะอาดแล้ว

### 2. `parseExcel(file: File): Promise<RawDataRow[]>`
อ่านไฟล์ Excel และแปลงเป็นข้อมูลดิบ

**ความสามารถ:**
- รองรับไฟล์ Excel (.xlsx, .xls)
- อ่านทุกแผ่นงานใน workbook
- ตรวจจับแถวส่วนหัวอัตโนมัติ (ค้นหาคำว่า 'เลขใบสำคัญ', 'วันที่', 'ผู้ขาย' เป็นต้น)
- แปลงเป็น JSON format
- ประมวลผลโครงสร้าง Head/Line อัตโนมัติ

### 3. `processMultiTableData(cleanedData: CleanedDataRow[], filename: string): MultiTableData`
สร้างข้อมูลหลายตารางสำหรับฐานข้อมูล

**ผลลัพธ์:**
- `procurement_data`: ข้อมูลการจัดซื้อที่ทำความสะอาดแล้ว (Line data)
- `procurement_head`: ข้อมูลส่วนหัว PO (Head rows)
- `procurement_line`: ข้อมูลรายการ PO (Line rows)
- `suppliers_master`: รายชื่อผู้ขายที่ไม่ซ้ำกัน
- `categories_master`: หมวดหมู่ที่ไม่ซ้ำกัน
- `upload_logs`: บันทึกการอัปโหลด
- `dataBySheet`: ข้อมูลจัดกลุ่มตามแผ่นงาน

### 4. `processHeadLineStructure(rawData: RawDataRow[]): RawDataRow[]`
ประมวลผลโครงสร้างข้อมูล Head/Line จาก Excel

**ความสามารถ:**
- แยกข้อมูล Head rows (มีเลข PO) และ Line rows (ไม่มีเลข PO)
- ผสานข้อมูลจาก Head ลงใน Line rows
- รองรับหลายแผ่นงานในไฟล์ Excel
- ตรวจจับแถวส่วนหัวอัตโนมัติ

## การตรวจสอบความถูกต้องของข้อมูล

### `isValidRow(row: RawDataRow): boolean`
ตรวจสอบว่าแถวข้อมูลถูกต้องหรือไม่

**เงื่อนไขการตรวจสอบ:**
1. ต้องมีเลขที่ PO
2. ไม่ใช่ PO ที่ถูกยกเลิก (ไม่มีคำว่า "ยกเลิก" หรือ "cancelled")
3. สถานะไม่ใช่ "ยกเลิก" หรือ "cancelled"

**ชื่อคอลัมน์ที่รองรับสำหรับ PO:**
- เลขใบสำคัญ (ชื่อไทยจาก Excel)
- PO, PO_Number, เลขที่ PO, PO.NO., PO NO
- NO.PO, No.PO

## การประมวลผลข้อมูลแต่ละแถว

### `processRow(row: RawDataRow): CleanedDataRow`
แปลงข้อมูลดิบเป็นรูปแบบมาตรฐาน

### 1. การจัดการวันที่
**ฟังก์ชัน:** `parseDate(dateValue: any): string`

**ความสามารถ:**
- รองรับวันที่แบบ Excel serial numbers
- รองรับหลายรูปแบบ:
  - dd/MM/yyyy, MM/dd/yyyy
  - yyyy-MM-dd, dd-MM-yyyy
  - รูปแบบ / สำหรับ dd/MM/yyyy
- คืนค่าในรูปแบบ YYYY-MM-DD

**ชื่อคอลัมน์ที่รองรับ:**
- วันที่, DATE, Date, PO Date

### 2. การจัดการตัวเลข
**ฟังก์ชัน:** `parseNumber(value: any): number`

**ความสามารถ:**
- ลบตัวอักษรที่ไม่ใช่ตัวเลข
- รองรับเครื่องหมายลบและจุดทศนิยม
- คืนค่า 0 ถ้าไม่สามารถแปลงได้

### 3. การแยกฟิลด์ที่รวมกัน
**ฟังก์ชัน:** `splitCombinedField(value: string)`

แยกข้อมูลที่รวมกันเช่น "ชื่อผู้ขาย - รายละเอียดสินค้า"

### 4. การจัดการภาษี
**ฟังก์ชัน:** `formatVat(value: string): string`

- ลบตัวอักษรที่ไม่ใช่ตัวเลข
- เพิ่มเครื่องหมาย % ตามหลัง
- คืนค่า "7%" ถ้าไม่มีข้อมูล

### 5. การจัดหมวดหมู่อัตโนมัติ
**ฟังก์ชัน:** `autoCategorize(description: string): string`

**กฎการจัดหมวดหมู่ (ปรับปรุงล่าสุด):**
- **Material**: วัสดุก่อสร้าง, อุปกรณ์ไฟฟ้า, เครื่องมือช่าง, อุปกรณ์ IT, อุปกรณ์สำนักงาน, เฟอร์นิเจอร์, คอมพิวเตอร์, ซอฟต์แวร์, CCTV, กล้องวงจร
- **Service**: ค่าแรง, บริการ, ติดตั้ง, ซ่อมบำรุง, บริการขนส่ง, บริการที่ปรึกษา, อบรม, บริการไอที, ทำความสะอาด, รักษาความปลอดภัย
- **Other**: หมวดหมู่เริ่มต้นถ้าไม่ตรงกับเงื่อนไขอื่น

## ชื่อคอลัมน์ที่รองรับ

### ข้อมูลพื้นฐาน
- **วันที่:** วันที่, DATE, Date, PO Date
- **เลขที่ PO:** เลขใบสำคัญ, PO, PO_Number, เลขที่ PO, PO.NO., PO NO, NO.PO, No.PO
- **ผู้ขาย:** ผู้ขาย, Supplier, ชื่อผู้ขาย, Supplier Name, Vendor
- **รายละเอียด:** คำอธิบาย, Description, รายละเอียด, รายการ, Item Description, Item_Description

### ข้อมูลโครงการ
- **โครงการ:** รหัสงาน, Project, Project Code, โครงการ, Project_Code, ProjectNo

### ข้อมูลจำนวนและราคา
- **จำนวน:** จำนวน, Qty, Quantity
- **ราคา:** ราคาสินค้า, Price, Amount, ราคา, จำนวนเงิน, Unit_Price, Total Amount, TotalValue

### ข้อมูลอื่นๆ
- **ภาษี:** VAT, ภาษี, VAT_Rate
- **วิศวกร:** Engineer, ผู้อนุมัติ, วิศวกร, Engineer_Name, __EMPTY_13, __EMPTY_14
- **หมวดหมู่:** Category, หมวดหมู่, ประเภท, __EMPTY_15, __EMPTY_16, __EMPTY_17, __EMPTY_18

## การแก้ไขปัญหาที่พบบ่อย

### 1. ข้อมูลไม่ถูกอ่าน
- **สาเหตุ:** ชื่อคอลัมน์ไม่ตรงกับที่รองรับ
- **วิธีแก้:** เพิ่มชื่อคอลัมน์ใน `findValue` function

### 2. วันที่ผิดรูปแบบ
- **สาเหตุ:** รูปแบบวันที่ไม่รองรับ
- **วิธีแก้:** เพิ่มรูปแบบใน `parseDate` function

### 3. ตัวเลขไม่ถูกต้อง
- **สาเหตุ:** มีตัวอักษรหรือสัญลักษณ์ปน
- **วิธีแก้:** ตรวจสอบ `parseNumber` function

### 4. ข้อมูลถูกกรองออก
- **สาเหตุ:** ไม่มีเลข PO หรือถูกยกเลิก
- **วิธีแก้:** ตรวจสอบ `isValidRow` function

## การปรับแต่ง

### การเพิ่มชื่อคอลัมน์ใหม่
```typescript
const poValue = this.findValue(row, [
  "เลขใบสำคัญ", // ชื่อไทยจาก Excel
  "PO",
  "PO_Number",
  "เลขที่ PO",
  "PO.NO.",
  "PO NO",
  "NO.PO",
  "No.PO",
  "ชื่อคอลัมน์ใหม่", // เพิ่มตรงนี้
]);
```

### การเพิ่มรูปแบบวันที่
```typescript
// รองรับ Excel serial dates อัตโนมัติ
// รูปแบบที่รองรับใน parseDate:
const formats = [
  "dd/MM/yyyy",
  "MM/dd/yyyy",
  "yyyy-MM-dd",
  "dd-MM-yyyy",
  // รูปแบบ dd/MM/yyyy สำหรับค่าที่มี /
];
```

### การปรับกฎการจัดหมวดหมู่
```typescript
// ในฟังก์ชัน autoCategorize
const categories: { [key: string]: string[] } = {
  'Material': [
    'วัสดุ', 'เหล็ก', 'ปูน', 'ไม้', 'อิฐ', // วัสดุก่อสร้าง
    'อุปกรณ์ไฟฟ้า', 'ประปา', 'ฮาร์ดแวร์', // อุปกรณ์ไฟฟ้า
    'computer', 'laptop', 'server', 'คอม', // IT Equipment
    'โต๊ะ', 'เก้าอี้', 'furniture', // เฟอร์นิเจอร์
    // เพิ่ม keyword ใหม่ตรงนี้
  ],
  'Service': [
    'ค่าแรง', 'labor', 'service', 'บริการ', // บริการทั่วไป
    'ติดตั้ง', 'ซ่อม', 'บำรุง', // บริการซ่อมบำรุง
    // เพิ่ม keyword ใหม่ตรงนี้
  ]
};
```

## ตัวอย่างการใช้งาน

```typescript
// อ่านไฟล์ Excel
const rawData = await DataCleaningService.parseExcel(file);

// ทำความสะอาดข้อมูล
const cleanedData = DataCleaningService.cleanData(rawData);

// สร้างข้อมูลหลายตาราง
const multiTableData = DataCleaningService.processMultiTableData(
  cleanedData, 
  "filename.xlsx"
);
```

## ข้อควรระวัง

1. **ตรวจสอบข้อมูลเสมอ** หลังจากทำความสะอาดเพื่อให้แน่ใจว่าถูกต้อง
2. **สำรองข้อมูล** ก่อนการประมวลผล
3. **ทดสอบกับข้อมูลจริง** เพื่อให้แน่ใจว่าฟังก์ชันทำงานถูกต้อง
4. **ตรวจสอบ log** ที่ console สำหรับ debugging

## Log ที่มีประโยชน์

ระบบจะแสดง log ที่ console ในรูปแบบ:
- `🧹 [DataCleaningService]` - เริ่มต้นการทำงาน
- `📊 [DataCleaningService]` - ข้อมูลสถิติและการวิเคราะห์
- `📋 [DataCleaningService]` - การประมวลผลแผ่นงาน
- `🔧 [DataCleaningService]` - การประมวลผลโครงสร้าง Head/Line
- `🔍 [DataCleaningService]` และ `🔍 [findValue]` - การตรวจสอบและค้นหาค่า
- `✅ [DataCleaningService]` - สำเร็จ
- `❌ [DataCleaningService]` - ข้อผิดพลาด
- `⚠️ [DataCleaningService]` - คำเตือน
- `⏭️ [DataCleaningService]` - การข้ามแถว
- `📅 [DataCleaningService]` - การแปลงวันที่

## คุณสมบัติใหม่ล่าสุด

### 1. รองรับหลายแผ่นงาน (Multi-sheet)
- อ่านทุกแผ่นงานในไฟล์ Excel
- จัดกลุ่มข้อมูลตามแผ่นงานต้นทาง
- บันทึกข้อมูลการประมวลผลแต่ละแผ่น

### 2. โครงสร้าง Head/Line อัตโนมัติ
- ตรวจจับ Head rows (มีเลข PO)
- ผสานข้อมูลจาก Head ลง Line rows
- สร้างตาราง `procurement_head` และ `procurement_line` แยกกัน

### 3. ตรวจจับแถวส่วนหัวอัตโนมัติ
- ค้นหาคำสำคัญภาษาไทยและอังกฤษ
- ข้ามแถวที่เป็นชื่อคอลัมน์อัตโนมัติ

### 4. รองรับ Excel Serial Dates
- แปลงวันที่แบบ Excel numbers เป็น YYYY-MM-DD
- รองรับวันที่ที่เป็นตัวเลขจาก Excel

---

**หมายเหตุ:** คู่มือนี้อ้างอิงจากไฟล์ `src/services/dataCleaning.ts` ปรับปรุงล่าสุด
