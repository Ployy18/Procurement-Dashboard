# คู่มือการติดตั้งระบบจัดเก็บข้อมูลแบบ Multi-Table (Node.js Backend)

คู่มือนี้จะช่วยให้คุณเปลี่ยนจากการใช้ Google Apps Script มาเป็นระบบ **Node.js Backend** ซึ่งจะทำให้การบันทึกข้อมูลลง Google Sheets ทำได้เร็วขึ้น เสถียรขึ้น และรองรับไฟล์ Excel ได้โดยตรงครับ

---

### 1. การเตรียมสิทธิ์เข้าถึง (Google Service Account)

เนื่องจากเราไม่ใช้ Apps Script แล้ว เราจึงต้องมี "บัญชีตัวแทน" (Service Account) เพื่อให้เซิร์ฟเวอร์ Node.js เข้าไปเขียนข้อมูลใน Google Sheets แทนเราครับ

**ขั้นตอนการสร้าง:**

1. ไปที่ [Google Cloud Console](https://console.cloud.google.com/)
2. สร้าง **Project** ใหม่ (หรือใช้ที่มีอยู่แล้ว)
3. ไปที่เมนู **APIs & Services > Library** แล้วเสิร์ชหา **"Google Sheets API"** จากนั้นกด **Enable**
4. ไปที่เมนู **Credentials > Create Credentials** เลือก **Service Account**
5. ตั้งชื่อบัญชี (เช่น `procurement-bot`) แล้วกด **Create and Continue**
6. เมื่อสร้างเสร็จ ให้คลิกไปที่ตัวบัญชีนั้น แล้วเลือกแถบ **Keys > Add Key > Create new key** เลือกรูปแบบ **JSON**
7. คุณจะได้ไฟล์ `.json` โหลดลงเครื่อง ให้เปิดดูข้อมูลในนั้นเพื่อนำไปใส่ในขั้นตอนถัดไป

**⚠️ สำคัญที่สุด:**
ให้คัดลอก **Email** ของ Service Account (เช่น `procurement-bot@...iam.gserviceaccount.com`) แล้วไปที่ Google Sheet ของคุณ จากนั้นกด **Share (แชร์)** และเพิ่ม Email นี้เข้าไปโดยให้สิทธิ์เป็น **Editor (ผู้แก้ไข)**

---

### 2. การตั้งค่าไฟล์ `.env`

เปิดไฟล์ `.env` ในโฟลเดอร์โปรเจกต์ของคุณ และใส่ข้อมูลจากไฟล์ JSON ที่โหลดมา ดังนี้:

```env
# URL ของ Backend (ปกติใช้พอร์ต 3001)
VITE_BACKEND_API_URL=http://localhost:3001

# ID ของ Google Sheet (ดูได้จาก URL ของชีทคุณ)
SPREADSHEET_ID=1ZmrD930kvZOcO9LSw0URFdl9KV35pcaUIXR5kuOCaD4

# Email ของ Service Account ที่สร้างในข้อ 1
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com

# Private Key (คัดลอกจากไฟล์ JSON ต้องครอบด้วยเครื่องหมายคำพูด " ")
# Tip: คีย์มักจะมีหลายบรรทัด ให้ก๊อปมาทั้งหมด
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour-Key-Here\n-----END PRIVATE KEY-----\n"
```

---

### 3. การรันระบบ (Server & UI)

เปิด Terminal หรือ Command Prompt ในโฟลเดอร์โปรเจกต์ แล้วรันคำสั่งตามลำดับ:

1. **ติดตั้งโปรแกรมที่จำเป็น:**

   ```bash
   npm install
   ```

2. **เริ่มทำงานระบบหลังบ้าน (Backend):**

   ```bash
   npm run server
   ```

   _หากสำเร็จ คุณจะเห็นข้อความ: `Backend server running on http://localhost:3001`_

3. **รันหน้าเว็บ (Frontend):** (เปิดอีกหน้าต่าง Terminal)
   ```bash
   npm run dev
   ```

---

### 4. วิธีการใช้งานและทดสอบ

1. เปิดหน้าเว็บ Dashboard ไปที่เมนู **Data Upload**
2. ลากไฟล์ **Excel (.xlsx)** หรือ **CSV** ที่ต้องการอัปโหลดลงในช่องวางไฟล์
3. ระบบจะแสดงพรีวิวข้อมูลที่ Clean แล้ว ให้ตรวจสอบความถูกต้อง
4. กดปุ่ม **"Confirm & Save to Sheets"**
5. รอสักครู่ หากสำเร็จ ระบบจะแจ้งว่าบันทึกข้อมูลลงทั้ง 4 ตารางเรียบร้อยแล้ว
6. เข้าไปดูใน Google Sheet ของคุณ จะพบข้อมูลแยกตามแผ่นงาน: `procurement_data`, `suppliers_master`, `categories_master`, และ `upload_logs`

---

### 💡 ข้อมูลเพิ่มเติม

- **ทำไมถึงดีกว่าเดิม?**: ระบบใหม่ส่งข้อมูลตรงไปยัง Google API ไม่ต้องผ่าน Apps Script ทำให้ไม่มีปัญหาเรื่อง Timeout และจัดการข้อมูลหลายตาราง (Multi-Table) ได้แม่นยำกว่า
- **ความปลอดภัย**: ข้อมูล Private Key ถูกเก็บไว้ที่ฝั่ง Server (Backend) เท่านั้น ไม่ได้หลุดไปที่หน้าเว็บ ทำให้ปลอดภัยจากการถูกขโมยข้อมูลครับ
